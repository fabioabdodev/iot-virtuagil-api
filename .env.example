# =========================================
# APPLICATION
# =========================================

NODE_ENV=development
PORT=3000

# =========================================
# DATABASE (Supabase / PostgreSQL)
# =========================================

DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
DIRECT_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
AUTH_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
AUTH_TOKEN_TTL_HOURS=168
AUTH_LOGIN_RATE_LIMIT_WINDOW_SECONDS=300
AUTH_LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5
AUTH_LOGIN_RATE_LIMIT_MAX_TRACKED_KEYS=10000
AUTH_LOGIN_LOCK_MINUTES=15
AUTH_PASSWORD_RESET_TTL_MINUTES=60
AUTH_PASSWORD_RETURN_LINK_IN_RESPONSE=false
WEB_APP_URL=https://monitor.virtuagil.com.br
RESEND_API_KEY=
RESEND_FROM_EMAIL=no-reply@virtuagil.com.br
TURNSTILE_SECRET_KEY=
TURNSTILE_VERIFY_URL=https://challenges.cloudflare.com/turnstile/v0/siteverify
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# =========================================
# DEVICE SECURITY
# =========================================

# API Key usada pelos dispositivos IoT (ESP32)
DEVICE_API_KEY=CHANGE_ME

# =========================================
# N8N INTEGRATION
# =========================================

# Webhook usado para enviar alertas de dispositivos offline
N8N_OFFLINE_WEBHOOK_URL=https://workflows.virtuagil.com.br/webhook/device-offline

# Webhook usado para avisar quando um dispositivo voltou a comunicar
N8N_ONLINE_WEBHOOK_URL=https://workflows.virtuagil.com.br/webhook/device-online

# Webhook para alertas de temperatura fora do limite
N8N_TEMPERATURE_ALERT_WEBHOOK_URL=https://workflows.virtuagil.com.br/webhook/temperature-alert

# Webhook para alertas de energia fora da faixa (corrente/tensao/consumo)
N8N_ENERGY_ALERT_WEBHOOK_URL=https://webhookworkflow.virtuagil.com.br/webhook/energy-alert

# =========================================
# APPLICATION SETTINGS
# =========================================

# tempo em minutos para considerar dispositivo offline
DEVICE_OFFLINE_MINUTES=5

# intervalo do monitor (cron)
MONITOR_INTERVAL_SECONDS=60

# cooldown para repetir alerta de temperatura fora do limite
TEMPERATURE_ALERT_COOLDOWN_MINUTES=5

# rate limit por device (janela/quantidade)
DEVICE_RATE_LIMIT_WINDOW_SECONDS=60
DEVICE_RATE_LIMIT_MAX_REQUESTS=30
DEVICE_RATE_LIMIT_MAX_TRACKED_DEVICES=10000
CONNECTIVITY_FLAP_WINDOW_MINUTES=30
CONNECTIVITY_FLAP_THRESHOLD=3
CONNECTIVITY_INSTABILITY_ALERT_COOLDOWN_MINUTES=60
CACHE_TTL_SECONDS=15
ALERT_QUEUE_BATCH_SIZE=20
ALERT_QUEUE_RETRY_MAX=3
ALERT_QUEUE_RETRY_DELAY_MS=2000

# =========================================
# LOGGING
# =========================================

LOG_LEVEL=info
